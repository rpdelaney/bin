#!/usr/bin/env bash
#
# share a file via transfer.sh

set -u

main() {
  filename="$1"
  basename="$(basename "$filename")" || return

  if [[ -r "$filename" ]]; then
    pv "$filename" | curl -Ss --upload-file - https://transfer.sh/"$basename"
  fi
}

main "$@"

# EOF
