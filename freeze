#!/usr/bin/env bash
#
set -x
amixer sset Master off
xset dpms force off
xset +dpms
if type -a swaylock &>/dev/null ; then
  swaylock || exit
else
  xlock || exit
fi

while pgrep i3lock &> /dev/null; do
  sleep 2s
done

thaw
